import { Menu, LogOut } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
export default function TopBar({onMenuClick}){const{user,logout}=useAuth();const roles=(user?.roles||[]).map(r=>r.replace(/_/g,' ')).join(', ');return <header className="h-16 border-b border-[#242424] bg-[#111] flex items-center justify-between px-4 lg:px-6"><div className="flex items-center gap-3"><button onClick={onMenuClick} className="lg:hidden p-2 text-[#9ca3af]"><Menu className="h-5 w-5"/></button><div><p className="text-sm font-semibold text-[#f0f0f0]">TransitOps</p><p className="text-[10px] text-[#6b7280]">Smart Transport Operations</p></div></div><div className="flex items-center gap-3"><span className="hidden sm:inline-flex px-2.5 py-1 rounded-full text-[10px] font-semibold uppercase tracking-wider border text-amber-400 bg-amber-900/20 border-amber-900/40">{roles}</span><div className="text-right hidden md:block"><p className="text-xs font-medium text-[#d1d5db]">{user?.name}</p><p className="text-[10px] text-[#6b7280]">{user?.email}</p></div><button onClick={logout} title="Logout" className="p-2 rounded-lg text-[#6b7280] hover:text-red-400 hover:bg-red-900/10"><LogOut className="h-4 w-4"/></button></div></header>}
